import React from 'react';
import $t from 'prop-types';
import style from './style.less';

const Form = () => {
  return (
    <div className={style.form}>
      Form
    </div>
  );
};

Form.propTypes = {

};

export default Form;
